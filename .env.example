# API Keys
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here  # Optional: for Claude models
HUGGINGFACE_API_KEY=your-hf-api-key-here       # Optional: for HF Inference API

# Model Configuration (these override config file values)
# For API models (GPT, Claude):
TEACHER_MODEL=gpt-4
STUDENT_MODEL=gpt-4o
REFLECTION_LM=gpt-5

# For HuggingFace models via API:
# TEACHER_MODEL=huggingface/meta-llama/Llama-3.1-8B-Instruct
# STUDENT_MODEL=huggingface/mistralai/Mistral-7B-Instruct-v0.2
# REFLECTION_LM=gpt-5

# For local models via vLLM (uncomment and set):
# TEACHER_MODEL=Arc-Intelligence/ATLAS-8B-Thinking
# STUDENT_MODEL=Qwen/Qwen2.5-7B-Instruct
# VLLM_HOST=localhost
# VLLM_PORT=8765

# LiteLLM Configuration (optional)
# LITELLM_LOG=DEBUG                    # For debugging API calls
# LITELLM_CACHE=True                   # Cache responses
# LITELLM_CACHE_TYPE=disk             # Cache type

# Azure OpenAI (if using Azure instead of OpenAI):
# AZURE_API_KEY=your-azure-key
# AZURE_API_BASE=https://your-resource.openai.azure.com/
# AZURE_API_VERSION=2024-02-15-preview
# TEACHER_MODEL=azure/your-deployment-name